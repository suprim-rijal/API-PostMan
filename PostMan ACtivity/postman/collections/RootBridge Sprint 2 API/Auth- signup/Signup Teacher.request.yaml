$kind: http-request
url: "{{baseUrl}}/api/auth/signup"
method: POST
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "name": "Ram Shrestha",
      "email": "ram.{{$timestamp}}@example.com",
      "password": "secret1",
      "role": "Teacher"
    }
scripts:
  - type: afterResponse
    code: |-
      pm.test('Status is 2xx', () => pm.response.to.be.success);
      pm.test('success is true', () => pm.expect(pm.response.json().success).to.eql(true));
      pm.test('role is Teacher', () => pm.expect(pm.response.json().user.role).to.eql('Teacher'));
    language: text/javascript
order: 3000
